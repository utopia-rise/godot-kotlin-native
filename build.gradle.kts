subprojects {
    group = "com.utopia-rise"
    version = "0.1.0" //remember to bump this version in conjunction with the version in the CompilerPluginConst
    repositories {
        mavenLocal()
        jcenter()
    }
}